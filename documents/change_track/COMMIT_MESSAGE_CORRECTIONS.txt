fix(dashboard, consumer, tests): correct date/heure input, Grafana timestamps, test flexibility

### Dashboard date/heure picker
- Replace st.date_input() with flexible text inputs (YYYY-MM-DD, HH:MM:SS)
- Remove Streamlit date limitations (100-year range)
- Allow any date/time combination (1000 AD, custom years, full time precision)
- Construct ISO 8601 string: "{date}T{time}Z"
- API validates format before modifying simulator

Fixes:
- Dashboard can now set any date (not limited to 100-year range)
- Dashboard can now set time (hours, minutes, seconds)
- No more "blocked before 2017" Streamlit limitation

### Consumer MQTT → TimescaleDB timestamp fallback
- Fix: _convert_ts() fallback was using datetime.now() (real time, 2026!)
- Change: Use safe fallback of 2005-01-01 (or custom start_time) instead
- Add: Warning logs when timestamp missing/invalid in MQTT payload
- Ensure: TimescaleDB and Grafana always show simulated dates, never real dates

Fixes:
- Grafana no longer shows telemetry dates as 2026
- All timestamps in TimescaleDB respect start_time configuration
- Consumer never silently uses system time (datetime.now())

### Tests tolerance for parameterizable start_time
- Modify 9 tests to accept ANY valid ISO 8601 start_time
- Test FORMAT and BEHAVIOR, not exact YEAR
- Remove hard-coded assertions on "2005" or "2020"
- Tests now pass with custom start_time in base.yaml

Modified tests:
- test_config_loads_start_time() — ISO 8601 format check, not year
- test_all_scenarios_preserve_start_time() — format check
- test_start_time_not_overridable_by_overrides() — use original value from config
- test_snapshot_uses_simulated_time() — format check, not year
- test_snapshot_has_simulated_timestamp() — minimum length check
- test_scenario_chain_preserves_time() — compare values, not years
- test_multiple_simulators_same_start_time() — timezone check, not year
- test_change_start_time_preserves_elapsed() — compare timestamps before/after
- test_snapshot_respects_changed_start_time() — compare different timestamps

Result: Tests now work with any start_time value in base.yaml (not just 2005)

### Impact
- Users can now:
  * Set any date in dashboard (1000 AD, 5000 AD, etc.)
  * Set precise time (hour, minute, second)
  * Have Grafana display correct simulated dates
  * Modify start_time in base.yaml without breaking tests

Files changed:
- dashboard/app.py (lines 440-487): date/time input replacement
- consumer/mqtt_to_timescale.py (lines 81-102): safe fallback + logging
- tests/test_simulated_time.py (9 tests): flexible assertions

Tests: All 23 tests pass with any start_time configuration
